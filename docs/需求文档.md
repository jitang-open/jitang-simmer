# Jitang Simmer · 需求文档

> 创建时间：2026-08-20 23:46:40 · 最近更新：2026-08-23 23:32:24（v0.8.2 开机自启与时间范围增强）
> 状态说明：本文档记录系统待实现功能需求，按里程碑组织；「状态」列随开发推进更新。
> 优先级：P0 = 系统成立的前提；P1 = 有真实数据后最有价值；P2 = 锦上添花/工程保障。
> 协作规范：所有改动仅保留在本地；**只有所有者明确要求 push 时才能推送到 GitHub**。

---

## 〇、当前基线

当前基线为 v0.8.2：Windows 采集端、Rust Token sidecar、Node.js 后端、SQLite 存储和实时前端已经跑通软件时长与 AI Token 真实数据闭环；Token 来源现含 Codex、ZCode、DeepSeek Harness 和 WorkBuddy。采集器支持无需管理员权限的当前用户级开机自启。软件点阵按各图 P95 比例分档并以 5 小时为硬上限，Token 年度点阵以 1 亿 Tokens 为硬上限；点击日期格可下钻到当天，软件与 Token 卡片支持日/自然周/自然月/任意起止日期，Token 另有累计全部历史。Token 趋势可按多个模型同时绘制，并为模型分配稳定的独立颜色。设备支持服务端别名、暂停与恢复，别名不会被采集端覆盖。白名单及软件元数据由后端 SQLite 持久化；腾讯云 CVM 的 Node/Caddy/HTTPS/访问控制部署栈已就绪。真实硬件传感器采集仍待完成。

---

## 一、M1 · 数据采集端（Windows，P0）

| 编号 | 需求 | 说明 / 要点 | 优先级 | 状态 |
|------|------|-------------|--------|------|
| CAP-01 | 前台软件监听 | 轮询 `GetForegroundWindow`（2 秒间隔），按分钟聚合成「软件 × 分钟数」，非空闲采样过半才计入 | P0 | ✅ v0.6.0 |
| CAP-02 | 进程名映射表 | 白名单支持添加 / 编辑（名称·类型·颜色）/ 删除及后端同步；v0.7.1 起已观测进程使用可搜索、固定高度滚动列表，仍支持手动输入，软件类型使用统一自定义下拉 | P0 | ✅ v0.7.1 |
| CAP-03 | 空闲检测 | `GetLastInputInfo` 键鼠空闲（默认 5 分钟阈值，可配置）+ 锁屏事件（SessionSwitch）不计入时长 | P0 | ✅ v0.6.0 |
| CAP-04 | 托盘常驻 | 系统托盘静默运行、暂停/恢复、立即上报、设置对话框、运行日志；设置中可启停当前用户级 Windows 登录自启，路径改变后下次启动自动刷新，单实例互斥防止重复运行 | P0 | ✅ v0.8.2 |
| CAP-05 | 本地缓存与上报 | 逐分钟写入本地队列（原子写，断网不丢），定时批量上报（默认 5 分钟，联调期 1 分钟），失败自动重试 | P0 | ✅ v0.6.0 |
| CAP-06 | 硬件传感器采集 | CPU/GPU 占用与温度、内存/显存占用、整机功耗、硬盘占用；基于 LibreHardwareMonitor（需管理员权限，安装时引导授权）；失败时优雅降级（缺失指标不阻塞时长统计） | P0 | 未开始（M2） |
| CAP-07 | 多设备支持 | 每台设备独立 `deviceId`（配置文件 `%APPDATA%\SimmerCollector\config.json`，含设备名/服务器地址/Token） | P0 | ✅ v0.6.0 |

## 二、M1 · 存储与后端（P0）

| 编号 | 需求 | 说明 / 要点 | 优先级 | 状态 |
|------|------|-------------|--------|------|
| SRV-01 | 上报 API | `POST /api/ingest`，Bearer token 鉴权；逐行幂等（`ON CONFLICT DO NOTHING`，重复上报无害） | P0 | ✅ v0.6.0 |
| SRV-02 | 数据存储 | SQLite（`server/data/simmer.db`，WAL 模式）：`usage_minutes` 逐分钟表 + `devices` 设备表；小时/天聚合由查询层即时完成 | P0 | ✅ v0.6.0 |
| SRV-03 | 查询聚合接口 | `/api/year` `/api/trend` `/api/app-totals` `/api/app-weekday` `/api/devices`，与 `data.js` 导出同构，前端图表层零改动 | P0 | ✅ v0.6.0 |
| SRV-04 | 年份范围接口 | `/api/range` 返回真实数据最早/最晚日期，前端年份下拉由数据驱动 | P0 | ✅ v0.6.0 |
| SRV-05 | 白名单后端同步 | 白名单与进程映射表持久化到 SQLite，连接同一后端的浏览器、origin 与设备共享；旧 localStorage 自动迁移并保留为缓存 | P1 | ✅ v0.6.6 |
| SRV-06 | 轻量部署方案（备选） | 评估「无传统后端」路线：设备端直接把聚合文件推到私有存储（私有仓库 / 对象存储），前端拉取解析——省去服务器运维，需评估安全性与一致性 | P1 | 待评估 |
| SRV-07 | 设备状态控制 | `PATCH /api/devices/:id` 持久化显示名和暂停状态；暂停期间服务端丢弃该设备新上报的软件分钟与 Token 事件，历史不删除 | P1 | ✅ v0.8.0 |

## 三、M3 · 前端功能增强（P1）

| 编号 | 需求 | 说明 / 要点 | 优先级 | 状态 |
|------|------|-------------|--------|------|
| FE-01 | 实时仪表 | 顶部或独立区块展示「此刻」：当前前台软件、实时 CPU/GPU/温度/功耗；30–60 秒轮询或 SSE | P1 | 未开始 |
| FE-02 | 活跃时段热力图 | 星期 × 小时双维度矩阵（GitHub 风格），直观呈现一周使用节律；支持按时长/次数切换 | P1 | 未开始 |
| FE-03 | 连续天数与成就 | 当前/最长连续使用天数 streak、最长单日、累计时长里程碑（100h/1kh/5kh…）；成就沿用点阵图视觉 | P1 | 未开始 |
| FE-04 | 软件分组 | 自定义分类（工作/娱乐/学习…），按分组统计占比；饼图支持按组下钻 | P1 | 未开始 |
| FE-05 | 软件资料编辑 | ✅ 已支持别名、类型、颜色和随类型变化的图标，并持久化到后端；多个进程合并为同一条目仍待实现 | P1 | 🚧 v0.6.6 部分完成 |
| FE-06 | 白名单通配符 | 支持通配符/正则规则（如 `steamapps/*` 一条规则匹配全部游戏进程）；规则命中测试工具 | P1 | 未开始 |
| FE-07 | 同比环比 | 本周 vs 上周、本月 vs 上月、今年 vs 去年；趋势图叠加对比曲线或差异标注 | P1 | 未开始 |
| FE-08 | 数据导出 | CSV / JSON 导出任意范围数据，便于备份与二次分析 | P1 | 未开始 |
| FE-09 | 隐私暂停 | 侧边栏可逐设备暂停/恢复；暂停在服务端生效且不删除历史。顶部总开关与敏感软件模糊显示仍可后续补充 | P1 | 🚧 v0.8.0 部分完成 |
| FE-10 | 网络流量统计（可选） | 上传/下载量按日/月统计，是否纳入视采集难度再定 | P2 | 待定 |
| FE-11 | 键鼠活动量（可选） | 按键次数、鼠标移动距离（WhatPulse 风格），趣味性指标 | P2 | 待定 |
| FE-12 | 自定义日期范围 | 软件与 Token 卡片使用统一暗色下拉日历，可选任意日、自然周、自然月或任意起止日期；Token 可切换累计全部历史；短范围趋势按日、超过 62 天按月聚合 | P1 | ✅ v0.8.2 |
| FE-13 | 单日下钻 | 点击软件或 Token 年度点阵格后切到该日并刷新对应明细；硬件曲线待 CAP-06 接入 | P1 | 🚧 v0.8.0 部分完成 |
| FE-14 | 多设备对比视图 | 各设备时长/硬件指标并排对比（对比柱状图或分色折线），弥补现有「仅汇总或单台」的不足 | P1 | 未开始 |
| FE-15 | 软件搜索与筛选 | 软件列表搜索框 + 按分类过滤，软件数量增多后必备 | P1 | 未开始 |
| FE-16 | 使用目标与提醒 | 每日/每周目标时长（如「编程 ≥ 3h/天」）进度条展示；反向支持娱乐类软件限时提醒（防沉迷） | P1 | 未开始 |
| FE-17 | 久坐提醒 | 连续使用超阈值弹通知提醒休息（需采集端系统通知配合） | P2 | 未开始 |
| FE-18 | 亮色主题切换 | 深/浅主题切换（GitHub light 模式风格），偏好持久化 | P2 | 未开始 |
| FE-19 | 今日时间线 | 今天用过的软件按时序排成时间轴（需采集端保留细粒度切换记录） | P2 | 未开始 |
| FE-20 | 硬件异常标记 | 温度/功耗曲线峰值自动标红提示 | P2 | 未开始 |
| FE-21 | 今日 vs 平均 | 今日曲线叠加过去 30 天平均曲线，直观对比当日状态 | P2 | 未开始 |
| FE-22 | 设备管理页 | 各设备在线状态、最后上报时间、数据量；SRV-01 已完成，仍需补充设备状态查询与独立页面 | P2 | 未开始 |
| FE-23 | 数据健康标记 | 区分「未开机」与「采集故障」的缺失日，点阵图上以不同样式标记（依赖采集端上报状态） | P2 | 未开始 |
| FE-24 | Ctrl+K 命令面板 | 快速跳转软件 / 设备 / 日期 / 功能页，GitHub/VSCode 式交互 | P2 | 未开始 |
| FE-25 | 年度点阵比例色阶 | 总览及每个软件分别按自身有效数据的 P95 分档并以 5 小时硬封顶；Token 年度点阵以 1 亿 Tokens 硬封顶；Tooltip 保留真实值 | P1 | ✅ v0.8.2 |
| FE-26 | 统一选择与折叠交互 | 网页下拉框统一为设备选择器样式；Token 时间按钮与顶栏双向同步；清空白名单二次确认；总时长与 Token 趋势默认收起并可独立展开 | P1 | ✅ v0.7.1 |

## 四、M1.5 · Token Statistics / AI Token 统计（P1）

### TS-01 · 支持来源

当前支持 Codex CLI、ZCode、DeepSeek Harness 和 WorkBuddy。OpenCode 本体、DeepSeek API 直连、DeepSeek 网页/App、费用和账户额度不纳入本阶段。

OpenCode Go 中的 DeepSeek V4 Flash 经 Harness 调用后，按 DeepSeek Harness 来源和实际 provider/model 统计真实 Token；该数据不代表 OpenCode Go 套餐剩余额度。

### TS-02 · 本地来源发现

采集器启动时、每 30 分钟及用户手动触发时扫描本地来源：

- Codex：Simmer 手动路径 → `CODEX_HOME` → `%USERPROFILE%\\.codex`，检查 `sessions` 与 `archived_sessions`。
- DeepSeek Harness：Simmer 手动路径 → `DSH_HOME` → `%USERPROFILE%\\.dsh`，检查 `sessions` 下的 `session.jsonl` 与 `session.jsonl.zstd`。
- ZCode：Simmer 手动路径 → `%USERPROFILE%\\.zcode`，检查 `cli\\db\\db.sqlite` 与 `projects`。
- WorkBuddy：Simmer 手动路径 → `WORKBUDDY_HOME` → `%USERPROFILE%\\.workbuddy`，只检查并监听 `projects/**/*.jsonl`，不监听频繁变化的配置、认证及 SQLite WAL。

可执行文件仅用于状态诊断，候选包括正在运行的进程、PATH、Windows App Paths、卸载注册表及已知用户目录；DSH 额外有界检查 npm 全局目录和 `npm-cache/_npx/*/node_modules/@deepseek-ai/dsh/package.json`，因此 `npx` 启动也能识别。禁止递归扫描整个磁盘。

每个来源显示 `ready`、`installed_no_data`、`history_only`、`not_found`、`incompatible` 或 `error`。同一来源默认只启用优先级最高的一个数据根，其他候选仅提示用户选择。

### TS-03 · 解析与采集

- 随采集器打包隐藏式 `simmer-token-scan.exe`，固定使用指定版本的 `tokscale-core`，保留请求级纯数字记录。
- 首次启动导入全部历史；文件变化后延迟约 5 秒增量扫描；每 15 分钟兜底扫描；每日完整校准。
- 增量扫描覆盖最近 48 小时，使用稳定事件 ID 去重；解析失败或 sidecar 超时不得影响软件时长采集。
- Codex 必须处理累计值差分、模型切换、乱序快照、计数回退、分叉会话、归档重复和残缺 JSONL 行。
- ZCode 必须兼容新旧 `model_usage` 字段、未完成记录、缓存/推理重叠和重复扫描。
- DeepSeek Harness 必须兼容普通 JSONL、Zstandard 多帧、残缺尾帧、子任务种子历史和重复消息。
- WorkBuddy 使用 `projects/**/*.jsonl` 的逐请求 `rawUsage`，不把 `workbuddy.db` 中会变化的累计快照当作新请求。

### TS-04 · 统一数据

每个请求级事件只保存：

- `sourceEventId`：稳定哈希。
- `source`：`codex`、`zcode`、`dsh` 或 `workbuddy`。
- `provider`、`model`、`occurredAt`。
- `inputTokens`：非缓存输入。
- `outputTokens`：非推理输出。
- `cacheReadTokens`、`cacheWriteTokens`、`reasoningTokens`。
- `totalTokens`：以上五类之和。

缓存 Token 和推理 Token 是明细桶，不得在总数中重复计算。

### TS-05 · 隐私与本地安全

严禁上传或持久化提示词、回答、代码、工具参数、项目路径、会话标题、Cookie、API Key、登录信息、`auth.json`、原始日志和原始 usage JSON。

采集器可以在本机读取日志中的必要字段，但服务端只接收规范化数字元数据和来源状态。离线队列保存在 `%APPDATA%\\SimmerCollector`，使用原子写入并自动重试。

### TS-06 · 后端接口与存储

新增 SQLite 表：

- `ai_token_events`：设备、来源、稳定事件 ID、provider、model、时间及 Token 数值。
- `ai_source_status`：设备、来源、状态、路径摘要、错误码和最后检查时间。

`ai_token_events` 使用 `(device_id, source, source_event_id)` 唯一约束，并为时间、来源、provider、model 建立索引。
已确认上传的事件没有 TTL 或自动清理：本地日志、工具安装或扫描路径以后消失时，SQLite 历史必须继续保留。“累计”的总 Token、请求数及排行读取全部历史；累计趋势仅将最近 12 个月作为可视化窗口，不影响累计数字。
来源当前不可用但数据库已有事件时，来源状态显示“历史已保存”；从未导入过事件的来源才显示“未找到”。

新增接口：

- `POST /api/ai-token-events`：Bearer 鉴权，批量接收 Token 事件和来源状态。
- `GET /api/ai-tokens/summary`、`trend`、`year`、`breakdown`、`sources`：提供汇总、趋势、年度热力图、排行和来源状态。

现有 `/api/ingest` 软件时长接口和数据库结构保持兼容。

### TS-07 · 前端展示

新增“AI Tokens”面板，包含 Token 分类概览、默认收起且可按需展开的日/周/月/自定义/累计趋势、年度独立比例热力图（最高按 1 亿 Tokens 顶格）、来源排行、模型排行，以及设备/来源/provider/model/时间范围筛选。来源、provider、model 使用统一自定义下拉；累计读取全部 SQLite 历史，自定义使用明确起止日期。

未安装、不兼容或没有数据时显示明确状态，不以零值冒充没有使用。`deepseek-v4-flash` 按实际模型名称展示，不冒充 OpenCode Go 套餐配额。

### TS-08 · 验收标准

- 首次运行可以导入 Codex、ZCode、DSH 和 WorkBuddy 的全部可读历史。
- 重复启动、重复扫描和断网重试不会重复计数。
- 本地日志被移动/删除或工具卸载后，已上传事件仍保留在后端累计统计中，来源状态变为“历史已保存”。
- 四种来源的 Token 分类与本地源数据一致，缓存和推理不会重复增加总数。
- 找到历史但程序不存在时仍能显示 `history_only` 并继续统计；找到程序但没有历史时显示 `installed_no_data`。
- 不同浏览器访问面板时看到同一份服务器 Token 数据。
- 不读取、存储或上传对话文本及认证信息。
- 现有软件时长、白名单、热力图和采集器功能无回归。

### TS-09 · 明确不做

- OpenCode 独立数据库适配。
- DeepSeek 直连 API 代理或调用方 SDK。
- DeepSeek 网页/App Token 估算。
- 账户额度、订阅周期和费用统计。
- 多个同类安装根目录的自动合并。
- 跨设备原始会话详情同步。

### TS 实施进度 · v0.8.1（2026-08-23）

- ✅ **TS-01～TS-08 已实现并真实验收**：Codex/ZCode/DSH/WorkBuddy 来源发现、固定版本请求级解析、五类互斥 Token、隐私边界、幂等存储/API 与前端 Token 面板已接通。
- ✅ **Codex / ZCode 真实环境验收**：首次全量扫描识别 Codex 41 个文件和 ZCode 1 个数据库；真实页面可按来源与模型筛选，跨浏览器读取同一份 SQLite 数据。
- ✅ **幂等验收**：同一批 500 条真实事件连续重传两次，服务端两次均返回 `inserted=0`、`duplicates=500`。
- ✅ **DSH npx 真实验收**：`npx @deepseek-ai/dsh` 的进程名虽为 `node.exe`，采集器会在固定 npm 缓存位置识别包；真实 DSH 会话持续解析上传，状态已由黄色 `history_only/data_only` 修正为绿色 `ready/data_and_app`。
- ✅ **WorkBuddy 真实验收**：4 个项目 JSONL 在 1.2 秒内解析出 166 次请求、12,436,438 Tokens；下一次增量扫描发现 0 个新文件并上传 0 条，服务端累计保持不变。
- 🔒 **隐私验收**：服务端只接收来源、provider、model、时间、稳定哈希和数字 Token 桶；程序路径、会话 ID、项目路径、提示词、回答、认证信息和原始 usage 均不进入数据库或上报队列。
- ✅ **v0.7.1 交互验收**：网页原生 `select` 已归零；Token 三个筛选下拉、面板内时间按钮、进程搜索滚动、手动进程、软件类型下拉、清空二次确认及两处趋势折叠均通过真实页面点击测试。
- ✅ **累计历史验收**：本地来源不可用只改变状态，不删除 SQLite 事件；v0.8.1 数据库迁移前自动备份并完整保留旧记录，WorkBuddy 重扫仍稳定为 12,436,438 Tokens / 166 次请求。
- ✅ **v0.8.0 交互验收**：软件和 Token 日期入口均为统一下拉日历；点阵点击、日/自然周/自然月查询、Token 多模型折线与稳定独立配色已经接通。
- ✅ **v0.8.2 时间范围**：软件和 Token 均可选择任意起止日期，Token 新增累计全部历史；短自定义趋势按日、超过 62 天按月聚合。软件点阵最高按 5 小时计算，Token 点阵最高按 1 亿 Tokens 计算。

## 五、M4 · 工程化（P2）

| 编号 | 需求 | 说明 / 要点 | 优先级 | 状态 |
|------|------|-------------|--------|------|
| ENG-01 | 远程访问部署 | 面向用户现有腾讯云 CVM 提供 Node.js 24 + Caddy Docker Compose、自动 HTTPS、面板 Basic Auth、采集 API Bearer Token 与持久卷；实际部署需域名与服务器权限 | P2 | ✅ v0.8.0 工程化就绪 |
| ENG-02 | 本机安装 git | 使用系统 Git 完成本地版本管理 | P2 | ✅ 2026-08-22（Git 2.55） |
| ENG-03 | 单元测试 | Node 端 17 项测试覆盖任意日/周/月/起止日期、长范围按月聚合、5 小时色阶封顶、WorkBuddy Token 幂等/筛选/累计、多模型分组、设备改名/暂停/恢复；Rust sidecar 4 项测试覆盖稳定哈希、五桶总数、时间转换和 WorkBuddy 来源隔离。采集端空闲剔除及独立来源发现测试仍待补齐 | P2 | 🚧 v0.8.2（Node 17 + Rust 4） |
| ENG-04 | 采集端自动更新 | 采集程序版本检查与自更新，避免多台机器手工升级 | P2 | 未开始 |

---

## 六、里程碑与建议顺序

1. **M1 最小闭环（已完成）**：CAP-01～05/07 + SRV-01～05 已接入真实数据，v0.8.2 完成开机自启收尾。
2. **M1.5 Token Statistics（已完成）**：Codex/ZCode/DSH/WorkBuddy 来源发现、请求级解析、后端幂等存储和 Token 面板均已完成真实验收。
3. **M2 硬件补全**：优先 CAP-06，并保证传感器不可用时不影响软件时长主链路。
4. **M3 前端增强**：FE-01 实时仪表 → FE-02 时段热力图 → FE-03 成就 → 其余按兴趣。
5. **M4 工程化**：腾讯云部署栈与 Node.js 24 基线已完成；后续补自动更新与采集端测试，实际公开部署仍需服务器权限与域名。

## 七、明确不做（Out of Scope）

以下功能经讨论**明确排除**，不再重复评估：

- ❌ 周报 / 年报分享卡片（一键生成图片）
- ❌ 电池健康统计（笔记本）
- ❌ 卡片显隐与拖拽排序
- ❌ PWA（添加到主屏 / 离线缓存）
- ❌ 洞察摘要卡（自动生成文字洞察）

## 八、分支约定

- `main`：稳定版本，仅收录阶段性完成的功能
- `feature/frontend`：前端进一步开发专用分支（FE 系列需求在此分支开发，完成后再合入 main）
- `feature/m1-core-loop`：M1 最小闭环分支，已合并到 `main`
- `feature/token-statistics`：Token Statistics 当前开发分支，TS 系列需求在此分支开发，完成后再合入 `main`
